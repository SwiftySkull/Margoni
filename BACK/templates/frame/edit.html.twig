{% extends 'base.html.twig' %}

{% block title %}Création d'un encadrement{% endblock %}

{% block frameActive %}active{% endblock %}

{% block body %}

<h1>Modification d'un encadrement</h1>

{{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}
    {# Génère tous les champs du form #}
    {{ form_widget(form) }}

    <input type="hidden" name="token" value="{{ csrf_token('add-edit-item') }}"/>
    <button type="submit" class="btn btn-success">Enregistrer</button>

{{ form_end(form) }}

{% endblock %}
